language: android
sudo: false
android:
  components:
  - build-tools-23.0.1
  - android-23
  - extra-android-support
  - extra-android-m2repository
script:
- ./gradlew build
after_deploy:
- fir p lib/build/outputs/aar/lib-release.apk -T $FIR_TOKEN -c "`git cat-file tag $TRAVIS_TAG`"
